package ui;

/*
import java.util.Scanner;

public class Main {

    public static void main(String[] args) {
        PacManGame game = new PacManGame();
        Level lvl = new Level(game);

        while (!game.isGameOver()) {
            Scanner scan = new Scanner(System.in);
            System.out.println("Whats the move?. Type new to start a new game, load to load the previous one, or");
            System.out.println("type help for the guide");
            String input = scan.nextLine();

            lvl.processCommand(input);
        }
    }

} */
